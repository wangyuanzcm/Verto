# Verto 数据库初始化操作指南

## 前提条件
- MySQL 和 Redis 已在 Docker 中正常启动
- MySQL 连接信息：
  - 主机：127.0.0.1
  - 端口：3306
  - 用户名：root
  - 密码：root
- Redis 连接信息：
  - 主机：127.0.0.1
  - 端口：6379
  - 密码：123456

## 数据库初始化步骤

### 方法一：使用 MySQL 命令行工具

1. **连接到 MySQL**
   ```bash
   mysql -h 127.0.0.1 -P 3306 -u root -p
   # 输入密码：root
   ```

2. **执行初始化脚本**
   ```sql
   source D:\github_workspace\Verto\database_init_manual.sql
   ```

### 方法二：使用 MySQL Workbench 或其他图形化工具

1. 打开 MySQL Workbench
2. 创建新连接：
   - 连接名：Verto Local
   - 主机名：127.0.0.1
   - 端口：3306
   - 用户名：root
   - 密码：root
3. 连接成功后，打开 `database_init_manual.sql` 文件
4. 执行整个脚本

### 方法三：使用 Docker 命令直接执行

```bash
# 进入 MySQL 容器
docker exec -it mysql_container_name mysql -u root -p

# 或者直接执行脚本文件
docker exec -i mysql_container_name mysql -u root -proot < D:\github_workspace\Verto\database_init_manual.sql
```

## 初始化内容说明

### 创建的数据库表

1. **app_manage** - 应用管理表
   - 存储应用信息、Git 仓库地址、管理员等

2. **staff** - 人员管理表
   - 存储员工信息、技能、工作地点等

3. **project** - 项目管理表
   - 存储项目需求、缺陷、开发进度等

4. **project_pipeline** - 项目流水线表
   - 存储构建、部署、测试流水线信息

5. **material_component** - 物料组件表
   - 存储可复用的 UI 组件、业务组件等

6. **material_template** - 物料模板表
   - 存储页面模板、项目模板等

### 测试数据

脚本会自动插入以下测试数据：
- 3 个应用管理记录
- 3 个人员记录
- 2 个项目记录
- 3 个流水线记录
- 2 个物料组件记录
- 2 个物料模板记录

## 验证初始化结果

执行完脚本后，会显示以下统计信息：
- 数据库名称：verto
- 各表的记录数量

你也可以手动查询验证：
```sql
USE verto;
SHOW TABLES;
SELECT COUNT(*) FROM app_manage;
SELECT COUNT(*) FROM staff;
SELECT COUNT(*) FROM project;
SELECT COUNT(*) FROM project_pipeline;
SELECT COUNT(*) FROM material_component;
SELECT COUNT(*) FROM material_template;
```

## 注意事项

1. **字符编码**：所有表都使用 `utf8mb4` 字符集，支持中文和 emoji
2. **时间戳**：创建时间和更新时间会自动设置
3. **索引优化**：已为常用查询字段添加索引
4. **数据完整性**：设置了必要的唯一约束和外键关系

## 故障排除

### 常见问题

1. **连接失败**
   - 检查 Docker 容器是否正常运行
   - 确认端口映射是否正确

2. **权限问题**
   - 确保 root 用户有创建数据库的权限
   - 检查 MySQL 用户权限设置

3. **字符编码问题**
   - 确保客户端连接使用 utf8mb4 编码
   - 检查 MySQL 服务器字符集配置

### 重新初始化

如果需要重新初始化数据库：
```sql
DROP DATABASE IF EXISTS verto;
-- 然后重新执行初始化脚本
```

## 下一步

数据库初始化完成后，可以：
1. 启动 Verto 后端服务
2. 启动前端开发服务器
3. 访问应用进行功能测试