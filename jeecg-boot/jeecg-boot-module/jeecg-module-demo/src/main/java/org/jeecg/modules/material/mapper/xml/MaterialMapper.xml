<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.material.mapper.MaterialMapper">

    <!-- 根据物料名称查询 -->
    <select id="getMaterialByName" resultType="org.jeecg.modules.material.entity.Material">
        SELECT * FROM material WHERE name = #{name} AND del_flag = 0
    </select>

    <!-- 根据物料类型查询 -->
    <select id="getMaterialByType" resultType="org.jeecg.modules.material.entity.Material">
        SELECT * FROM material WHERE type = #{type} AND del_flag = 0 ORDER BY create_time DESC
    </select>

    <!-- 根据权限SQL查询数据集 -->
    <select id="queryListWithPermission" parameterType="Object" resultType="org.jeecg.modules.material.entity.Material">
        SELECT * FROM material WHERE del_flag = 0 ${permissionSql} ORDER BY create_time DESC
    </select>

    <!-- 根据物料名称和类型查询物料（用于唯一性校验） -->
    <select id="getMaterialByNameAndType" resultType="org.jeecg.modules.material.entity.Material">
        SELECT * FROM material 
        WHERE name = #{name} 
        AND type = #{type} 
        AND del_flag = 0
        <if test="excludeId != null and excludeId != ''">
            AND id != #{excludeId}
        </if>
        LIMIT 1
    </select>

    <!-- 获取物料统计信息 -->
    <select id="getMaterialStatistics" resultType="java.util.Map">
        SELECT 
            COUNT(*) as total,
            SUM(CASE WHEN status = '1' THEN 1 ELSE 0 END) as active,
            SUM(CASE WHEN status = '0' THEN 1 ELSE 0 END) as inactive,
            SUM(CASE WHEN type = 'component' THEN 1 ELSE 0 END) as components,
            SUM(CASE WHEN type = 'template' THEN 1 ELSE 0 END) as templates
        FROM material 
        WHERE del_flag = 0
    </select>

    <!-- 根据状态统计物料数量 -->
    <select id="countByStatus" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM material 
        WHERE status = #{status} AND del_flag = 0
    </select>

    <!-- 根据类型统计物料数量 -->
    <select id="countByType" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM material 
        WHERE type = #{type} AND del_flag = 0
    </select>

</mapper>